# import * from './generated/prisma.graphql'

type Mutation {
  createUser(data: UserCreateInput!): User!  
}

type Query {
  locations(where: LocationWhereInput, orderBy: LocationOrderByInput): [Location]!  
}
